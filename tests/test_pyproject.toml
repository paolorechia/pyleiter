[tool.poetry]
name = "pyleiter"
version = "0.1.0"
description = ""
authors = ["Paolo Rechia <paolorechia@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
toml = "^0.10.2"

[tool.poetry.group.dev.dependencies]
pytest = "^8.2.2"
tox = "^4.15.1"
ruff = "^0.5.0"

[pyleiter]
lint = { command = "ruff format && ruff check src && ruff check tests", help = "Runs project formatter and linter" }

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
